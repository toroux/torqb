local QBCore = exports['qb-core']:GetCoreObject()

-- Event to reload zones (kept for potential future use)
RegisterNetEvent('static-zones:reloadZones', function()
    -- Zones are loaded from config.lua on resource start
    -- This event can be used to trigger client-side reloads if needed
end)

